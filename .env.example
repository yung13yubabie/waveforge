# WaveForge environment variables — copy to `.env` and fill in.

# Phase-2 backend (Demucs stems / genre AI) — leave unset until deployed.
# When unset, those features stay disabled with an honest "需後端" placeholder.
# VITE_BACKEND_URL=https://your-backend.example.com
