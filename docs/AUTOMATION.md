# Automation status

Not implemented. Current parameter ramps smooth realtime UI changes; they are not recorded automation lanes. History and named presets capture static mastering state.

Future automation needs stable processor/parameter IDs, sample/PPQ time conversion, read/write/touch/latch semantics, one render/live scheduler and project persistence. No claim of automation parity is made by the existing static-state render parity suite.
